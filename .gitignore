# Build artifacts
bin/
dist/
# Locally compiled hfslice CLI binary (source lives in cmd/hfslice)
/hfslice

# Logs
logs/
*.log

# Package manager caches
.pnpm-store/
node_modules/

# Project uses npm — pnpm/yarn lock files would diverge from package-lock.json
pnpm-lock.yaml
yarn.lock

# MCP / tool caches
.playwright-mcp/

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Dev screenshots (root-level only)
/*.png

# Claude Code runtime state
.claude/*.lock
.superpowers/
