
!.vscode/extensions.json
.claude/*.lock
.design-mockups/
.DS_Store
.idea/
.playwright-mcp/
.pnpm-store/
.vscode/*
*.log
/*.png
# Build artifacts
# Claude Code runtime state
# Dev screenshots (root-level only)
# Logs
# MCP / tool caches
# OS / editor
# Package manager caches
# Project uses npm — pnpm/yarn lock files would diverge from package-lock.json
bin/
dist/
logs/
node_modules/
pnpm-lock.yaml
render-test-out/
yarn.lock
.superpowers/
.glm-briefs/

# Go build/module cache ที่ตั้งไว้ในโปรเจกต์ (ดู Global Constraints ในแผน retention)
.gocache-build/
.gocache-mod/
.dmux/
.worktrees/
